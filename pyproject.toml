[project]
name = "planet-superres"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "rasterio",
    "numpy",
    "pillow",
    "matplotlib",
    "scikit-image",
    "tqdm",
    "pandas",
    "jupyter",
    "ipykernel",
]
